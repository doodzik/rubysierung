require 'spec_helper'

describe Rubysierung do
  it 'has a version number' do
    expect(Rubysierung::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
